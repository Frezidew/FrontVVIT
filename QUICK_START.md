# Быстрый старт для развертывания

## Самый простой способ (Render.com)

### 1. Подготовка (5 минут)

```bash
# Убедитесь, что все файлы сохранены
git init
git add .
git commit -m "Ready for deployment"
```

### 2. Загрузка на GitHub

1. Создайте репозиторий на GitHub
2. Загрузите код:
```bash
git remote add origin https://github.com/ваш-username/movieworld.git
git push -u origin main
```

### 3. Развертывание на Render (10 минут)

1. Зайдите на [render.com](https://render.com) → Sign Up (через GitHub)
2. **Создайте базу данных:**
   - "New +" → "PostgreSQL" (или MySQL)
   - Name: `movieworld-db`
   - Plan: Free
   - Сохраните Internal Database URL

3. **Создайте веб-сервис:**
   - "New +" → "Web Service"
   - Подключите ваш GitHub репозиторий
   - Настройки:
     - **Root Directory**: `backend`
     - **Build Command**: `npm install`
     - **Start Command**: `npm start`
   - Environment Variables:
     ```
     DB_HOST=ваш-хост
     DB_USER=ваш-пользователь
     DB_PASS=ваш-пароль
     DB_NAME=movieworld
     PORT=10000
     ```
   - Нажмите "Create Web Service"

4. **Создайте статический сайт:**
   - "New +" → "Static Site"
   - Подключите тот же репозиторий
   - Root Directory: оставьте пустым
   - Build Command: оставьте пустым
   - Нажмите "Create Static Site"

5. **Инициализируйте БД:**
   - Подключитесь к БД через MySQL клиент
   - Выполните SQL из `backend/db.sql`

### 4. Готово! 🎉

Ваш сайт будет доступен по адресу, который предоставит Render.

---

## Альтернатива: Railway.app (еще проще)

1. Зайдите на [railway.app](https://railway.app) → Login with GitHub
2. "New Project" → "Deploy from GitHub repo"
3. Выберите ваш репозиторий
4. "+ New" → "Database" → "Add MySQL"
5. Railway автоматически настроит все!

---

## Если что-то не работает

1. Проверьте логи в панели управления хостинга
2. Убедитесь, что переменные окружения настроены правильно
3. Проверьте, что база данных создана и таблицы инициализированы
4. Проверьте `/api/health` endpoint

---

**Подробная инструкция:** см. `DEPLOYMENT.md`

