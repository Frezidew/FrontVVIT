# Инструкция по развертыванию на хостинге

## Варианты хостинга

### 1. Бесплатные хостинги с поддержкой Node.js

#### **Render.com** (рекомендуется для начала)
- ✅ Бесплатный тариф с поддержкой Node.js
- ✅ Автоматический деплой из GitHub
- ✅ Бесплатная MySQL база данных
- ✅ HTTPS из коробки
- ⚠️ Приложение "засыпает" после 15 минут бездействия (на бесплатном тарифе)

#### **Railway.app**
- ✅ Бесплатный тариф ($5 кредитов в месяц)
- ✅ Простой деплой
- ✅ Поддержка MySQL

#### **Heroku**
- ⚠️ Больше нет бесплатного тарифа, но есть пробный период

#### **Vercel / Netlify**
- ✅ Отлично для статических сайтов
- ⚠️ Ограниченная поддержка Node.js (serverless functions)

### 2. Платные хостинги

- **DigitalOcean** ($4-6/месяц)
- **AWS EC2** (pay-as-you-go)
- **Hostinger** (от $2.99/месяц)
- **Timeweb** (российский хостинг, от 200₽/месяц)

---

## Пошаговая инструкция для Render.com

### Шаг 1: Подготовка проекта

1. Убедитесь, что все файлы закоммичены в Git:
```bash
git init
git add .
git commit -m "Initial commit"
```

2. Создайте репозиторий на GitHub и загрузите код:
```bash
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
git push -u origin main
```

### Шаг 2: Создание базы данных на Render

1. Зайдите на [render.com](https://render.com) и зарегистрируйтесь
2. Нажмите "New +" → "PostgreSQL" (или MySQL, если доступен)
3. Выберите:
   - **Name**: `movieworld-db`
   - **Database**: `movieworld`
   - **User**: `movieworld_user`
   - **Region**: выберите ближайший
   - **Plan**: Free
4. Нажмите "Create Database"
5. **Сохраните** Internal Database URL (он понадобится позже)

### Шаг 3: Создание веб-сервиса

1. На Render нажмите "New +" → "Web Service"
2. Подключите ваш GitHub репозиторий
3. Настройте:
   - **Name**: `movieworld-backend`
   - **Root Directory**: `backend`
   - **Environment**: `Node`
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
4. В разделе "Environment Variables" добавьте:
   ```
   DB_HOST=ваш-хост-из-внутреннего-url
   DB_USER=ваш-пользователь
   DB_PASS=ваш-пароль
   DB_NAME=movieworld
   PORT=10000
   NODE_ENV=production
   ```
5. Нажмите "Create Web Service"

### Шаг 4: Настройка статических файлов

Render автоматически раздает статические файлы из корня проекта, но нужно настроить правильные пути.

**Вариант А**: Использовать отдельный статический сайт
1. На Render: "New +" → "Static Site"
2. Подключите тот же репозиторий
3. **Root Directory**: оставьте пустым (корень проекта)
4. **Build Command**: оставьте пустым
5. Нажмите "Create Static Site"

**Вариант Б**: Использовать один веб-сервис (проще)
- Статические файлы уже раздаются через `server.js` из корневой директории
- Просто убедитесь, что путь в `server.js` правильный

### Шаг 5: Инициализация базы данных

1. Подключитесь к базе данных через MySQL клиент или используйте Render Shell:
   - На странице базы данных нажмите "Connect"
   - Используйте предоставленные данные для подключения

2. Выполните SQL скрипт:
```bash
# Через Render Shell или локально с подключением к удаленной БД
mysql -h ваш-хост -u ваш-пользователь -p < backend/db.sql
```

Или выполните SQL вручную через MySQL Workbench или phpMyAdmin (если доступен).

### Шаг 6: Настройка URL API (если нужно)

Если статические файлы и API находятся на **одном домене** (что обычно и бывает), ничего делать не нужно - все будет работать автоматически.

Если API находится на **другом домене**, добавьте в HTML файлы (в тег `<html>`) атрибут:
```html
<html lang="ru" data-api-base="https://ваш-api-сервер.onrender.com">
```

Например, в `index.html`:
```html
<!DOCTYPE html>
<html lang="ru" data-api-base="https://movieworld-backend.onrender.com">
```

Если атрибут не указан, будет использоваться текущий домен (относительные пути).

---

## Альтернатива: Railway.app

### Шаг 1: Регистрация
1. Зайдите на [railway.app](https://railway.app)
2. Войдите через GitHub

### Шаг 2: Создание проекта
1. "New Project" → "Deploy from GitHub repo"
2. Выберите ваш репозиторий

### Шаг 3: Добавление MySQL
1. В проекте нажмите "+ New" → "Database" → "Add MySQL"
2. Railway автоматически создаст переменные окружения

### Шаг 4: Настройка переменных окружения
1. Перейдите в "Variables"
2. Добавьте:
   ```
   PORT=3000
   NODE_ENV=production
   ```
3. Railway автоматически добавит переменные для БД (MYSQL_HOST, MYSQL_USER, и т.д.)

### Шаг 5: Настройка деплоя
1. В настройках сервиса:
   - **Root Directory**: `backend`
   - **Start Command**: `npm start`

### Шаг 6: Инициализация БД
1. Используйте Railway CLI или подключитесь через MySQL клиент
2. Выполните `backend/db.sql`

---

## Настройка для российских хостингов (Timeweb, Beget)

### Timeweb

1. **Создайте аккаунт** на [timeweb.com](https://timeweb.com)

2. **Загрузите файлы через FTP/SFTP:**
   - Используйте FileZilla или другой FTP клиент
   - Загрузите все файлы в папку `public_html` или `www`

3. **Настройте Node.js приложение:**
   - В панели управления найдите "Node.js"
   - Создайте новое приложение
   - Укажите путь: `backend`
   - Start command: `npm start`

4. **Создайте базу данных:**
   - В панели управления → "Базы данных MySQL"
   - Создайте новую БД
   - Импортируйте `backend/db.sql`

5. **Настройте переменные окружения:**
   - В настройках Node.js приложения добавьте переменные из `.env`

### Beget

Аналогично Timeweb, но используйте панель управления Beget.

---

## Проверка после развертывания

1. **Проверьте работу API:**
   ```
   https://ваш-домен.com/api/health
   ```
   Должен вернуть JSON с информацией о статусе.

2. **Проверьте статические файлы:**
   ```
   https://ваш-домен.com/index.html
   ```

3. **Проверьте авторизацию:**
   - Попробуйте зарегистрироваться
   - Попробуйте войти

---

## Решение проблем

### Проблема: "Cannot find module"
**Решение**: Убедитесь, что в `package.json` все зависимости указаны в `dependencies`, а не в `devDependencies`.

### Проблема: База данных не подключается
**Решение**: 
- Проверьте переменные окружения
- Убедитесь, что БД создана и доступна
- Проверьте, что таблицы созданы (выполните `db.sql`)

### Проблема: Статические файлы не загружаются
**Решение**: 
- Проверьте путь в `server.js`: `path.join(__dirname, '..')`
- Убедитесь, что файлы загружены на сервер

### Проблема: CORS ошибки
**Решение**: Убедитесь, что `cors()` middleware подключен в `server.js`.

---

## Дополнительные файлы для деплоя

Создайте файл `.gitignore` (если его нет):
```
node_modules/
.env
*.log
.DS_Store
```

---

## Полезные ссылки

- [Render.com Documentation](https://render.com/docs)
- [Railway.app Documentation](https://docs.railway.app)
- [Node.js Best Practices](https://github.com/goldbergyoni/nodebestpractices)

---

## Примерный бюджет

**Бесплатные варианты:**
- Render.com: бесплатно (с ограничениями)
- Railway.app: $5 кредитов/месяц (хватает для тестирования)

**Платные варианты:**
- Timeweb: от 200₽/месяц
- DigitalOcean: от $4/месяц
- Hostinger: от $2.99/месяц

---

**Удачи с развертыванием! 🚀**

